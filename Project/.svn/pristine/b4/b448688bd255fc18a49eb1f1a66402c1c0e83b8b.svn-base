package Table;

/**
 * Handles comparison cases with no where clause.
 */
public class NoWhere implements WhereClause {

	/**
	 * Compares cases with no where clause.
	 * 
	 * @param rowNum
	 *            Current row number.
	 * @return true
	 */
	public boolean matches(int rowNum) {
		return true;
	}
}
